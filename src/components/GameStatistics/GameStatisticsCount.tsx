import styled from "@emotion/styled";
import { slate } from "~/ui-kit/colors";

export const GameStatisticsCount = styled("span")(() => ({
  fontSize: "1.5rem",
  fontWeight: 700,

  color: slate[500],
}));
